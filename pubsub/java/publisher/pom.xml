<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.7</version>
    <relativePath /> <!-- lookup parent from repository -->
  </parent>
  <groupId>com.service</groupId>
  <artifactId>publisher</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>publisher</name>
  <description> Catalyst Pubsub Quickstart: Publisher</description>
  <properties>
    <java.version>17</java.version>
    <dapr.version>1.17.0</dapr.version>
  </properties>
  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <!-- dapr-spring-boot-starter, NOT dapr-sdk-springboot. Controller injects
         DaprClient with @Autowired, and only this starter contributes that bean
         (io.dapr.spring.boot.autoconfigure.client.DaprClientAutoConfiguration).
         dapr-sdk-springboot's DaprAutoConfiguration declares no beans, so with
         that artifact the context fails to start with "required a bean of type
         'io.dapr.client.DaprClient' that could not be found". Matches state/java. -->
    <dependency>
      <groupId>io.dapr.spring</groupId>
      <artifactId>dapr-spring-boot-starter</artifactId>
      <version>${dapr.version}</version>
    </dependency>
    <dependency>
      <groupId>com.squareup.okhttp3</groupId>
      <artifactId>okhttp</artifactId>
      <version>4.9.2</version>
    </dependency>
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <version>1.18.30</version>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.json</groupId>
      <artifactId>json</artifactId>
      <version>20231013</version>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
